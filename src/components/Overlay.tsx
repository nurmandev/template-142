import { AbsoluteFill } from 'remotion';

const Overlay = () => {
  return (
    <AbsoluteFill
      style={{
        background: 'linear-gradient(135deg, #2b7df790 0%, #2b39f76c 100%)',
      }}
    />
  );
};

export default Overlay;
